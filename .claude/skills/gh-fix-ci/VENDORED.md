# Vendored skill: gh-fix-ci

Copied into this repository on 2026-07-23 from the maintainer's personal skills
directory (`~/.claude/skills/gh-fix-ci`) as part of feature 006.

This is a **copy**, not a link. Nothing synchronizes it with its source, so it
can drift. Refreshing it is a deliberate act: re-copy from the source and update
the date above. Recording provenance here is what makes that drift detectable at
all — the alternative is a directory nobody can date.
