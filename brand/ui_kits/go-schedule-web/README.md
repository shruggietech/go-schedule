# go-schedule web UI kit

Open `index.html` to view a compact task dashboard built entirely from the shipped tokens, logo masters, and components. It demonstrates expression fidelity, explicit lifecycle state, responsive layout, and accessible status labels.
