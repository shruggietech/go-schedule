# Quickstart

1. Export from the source daemon with `gosched bundle export -o automation.json`.
2. Review the v2 document and its exclusions.
3. Preview against the selected target, supplying local watcher paths for new watchers.
4. Resolve every conflict and explicitly apply the reviewed plan.
5. Configure or rotate new target trigger credentials and enable sources when ready.
