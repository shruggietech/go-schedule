# S086 operator workflow

After separately authorized tag/draft refresh and hosted staging, supply exact candidate bytes, public v1.1.1 MSI, portable PowerShell 7 ZIP, and offline WebView2 x64 installer with recorded source identities and hashes. Preparation creates fresh and upgrade launch files without launching or installing anything.

Launch fresh.wsb for attended installation. Verified inputs stage locally, prerequisites prepare, and diagnostics export automatically. Complete the existing native matrix and #229-#233 regressions. Launch upgrade.wsb in a reset session, install the public baseline, prepare representative tasks/history/theme, then install the candidate. Use separate Windows 11 normal-user and high/mixed-DPI environments where Sandbox cannot represent required conditions.

No generated output claims qualification success. Local implementation stops at pre-push authorization; tag replacement, staging, and promotion have separate boundaries.
